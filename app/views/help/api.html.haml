.row
  .col-md-2
    %h2
      = link_to help_path, class: 'btn btn-default btn-xs' do
        =fa_icon 'angle-left'
        = t 'help.back'
      %span.pull-right API
    %hr

    #ul.nav.nav-pills.nav-stacked
      - %w(budokop_sensors contexts experiments device_aggregations devices edge_nodes levees measurement_nodes measurements neosentio_sensors parameters pumps fiber_optic_nodes profiles sensors timelines threat_assessments results sections).sort.each do |file|
        = nav_tab :category, file do
          = link_to file.titleize, help_api_file_path(file)
  .col-md-10
    = preserve do
      = markdown File.read(Rails.root.join("doc", "api", "#{@category}.md"))
